/* ===================================================
   CantiumCode Portfolio — app.js
   Master reference file.

   All JavaScript has been modularized into separate
   component files inside ./components/JS/ — matching
   the same pattern as the CSS (./components/CSS/).

   Each module is loaded via <script> tags in index.html:

   SCRIPT                          PURPOSE
   ─────────────────────────────── ──────────────────────────────
   components/component-loader.js  Fetches & injects HTML sections
   components/particles.js         Canvas particle background
   components/JS/loader.js         Page loader animation
   components/JS/navbar.js         Navbar scroll, hamburger, highlight
   components/JS/tabs.js           Experience tab switching
   components/JS/scroll-reveal.js  Intersection Observer scroll fade-in
   components/JS/smooth-scroll.js  Smooth anchor scrolling
   components/JS/contact-form.js   Formspree contact form handler
   components/JS/typewriter.js     Hero tagline typewriter effect
   components/JS/resume.js         Resume button (open PDF)
   =================================================== */